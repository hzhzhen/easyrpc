group 'net.easyrpc'
version project_version

apply plugin: 'java'

dependencies {
    compile project(':request-io')
    compile project(':rpc-core')

    testCompile group: 'junit', name: 'junit', version: '4.11'
}
